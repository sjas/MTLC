apply plugin: "java"
apply plugin: "eclipse"
apply plugin: "application"
apply from: "https://raw.github.com/breskeby/gradleplugins/master/emmaPlugin/emma.gradle"

mainClassName = "de.mg.StartApp"

repositories {
    mavenCentral()
}

task wrapper(type: Wrapper) {
    gradleVersion = '1.5'
}
